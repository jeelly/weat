import { keyframes } from "styled-components";

export const ItemRotate = keyframes`
0% {transform: rotate(0deg); }
33% {transform: rotate(2deg); }
66% {transform: rotate(-2deg);}
100% {transform: rotate(0deg);}`
;